<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="navigate_with_google_map_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    
    <!-- 🟢 আপনার পুরাতন <queries> ট্যাগটি এখানে আপডেট করা হয়েছে -->
    <queries>
        <!-- ১. গুগল ম্যাপস অ্যাপ কুয়েরি করার জন্য -->
        <package android:name="com.google.android.apps.maps" />
        
        <!-- ২. গুগল ম্যাপস ইউআরএল স্কিম কুয়েরি করার জন্য -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" android:host="google.com" />
        </intent>

        <!-- ৩. ফ্ল্যাটার ইঞ্জিনের নিজস্ব প্রসেস টেক্সট পারমিশন (যা আগে থেকেই ছিল) -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
